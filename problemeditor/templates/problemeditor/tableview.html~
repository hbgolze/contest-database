{% extends "./base.html" %}

{% block content %}
<h2>My Tests</h2>
Choose a test category.
<div class="content">
<table class="table table-striped">
    <thead>
      <tr>
        <th>Test Category</th>
        <th>Number of Untagged Problems</th>
	<th>Number of Problems</th>
	<th>Number of Problems with no Solution</th>
      </tr>
    </thead>
    <tbody>
      {% for type in types %}
      {% with typeproblems=problems.filter.%}
      <tr>
        <td><a href="types/{{type.type}}">{{type.type}}</a></td>
        <td> 	</td>
        <td>{{test.problems.count}}</td>
        <td>{{test.created_date}}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</div>

{% endblock %}
