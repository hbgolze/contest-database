{% extends "./base.html" %}
{% block title %}Choose a Tag{% endblock %}

{% block content %}
<ol class="breadcrumb">
  <li><a href="/problemeditor/">Select Type</a></li>
  <li class="active">{{typelabel}}</li>
</ol>
<h2>Problem Editor</h2>
Tag the <a href="/problemeditor/untagged/{{type}}">{{num_untagged}} untagged problems</a>. <br>
Please select a tag.
<div class="content">
<table class="table table-striped">
    <thead>
      <tr>
        <th>Tag</th>
	<th>Number of Tagged Problems with no Solutions</th>
	<th>Number of Problems with Tag</th>
      </tr>
    </thead>
    <tbody>
      {% for tag,unsolution,num in rows %}
      
      <tr>
        <td><a href="{{tag}}">{{tag}}</a></td>
        <td>{{unsolution}}</td>
        <td>{{num}}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</div>

{% endblock %}
