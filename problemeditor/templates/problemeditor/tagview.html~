{% extends "./base.html" %}
{% block title %}Choose a Tag{% endblock %}


{% block content %}
<h2>Problem Editor</h2>
Tag the <a href="/problemeditor/untagged/{{type}}">{{num_untagged}} untagged problems</a>. <br>
Please select a tag or test.
<div class="content">
<table class="table table-striped">
    <thead>
      <tr>
        <th>Tag</th>
	<th>Number of Tagged Problems with no Solutions</th>
	<th>Number of Problems with Tag</th>
      </tr>
    </thead>
    <tbody>
      {% for tag,unsolution,num in rows %}
      
      <tr>
        <td><a href="{{tag}}">{{tag}}</a></td>
        <td>{{unsolution}}</td>
        <td>{{num}}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
<br>
<table class="table table-striped">
    <thead>
      <tr>
        <th>Test</th>
	<th>Number of Untagged Problems</th>
	<th>Number of Problems with no Solution</th>
	<th>Total Number of Problems</th>
      </tr>
    </thead>
    <tbody>
      {% for testlabel,untag,unsolution,num in rows2 %}
      
      <tr>
        <td><a href="/problemeditor/testlabel/{{testlabel}}">{{testlabel}}</a></td>
        <td>{{untag}}</td>
        <td>{{unsolution}}</td>
        <td>{{num}}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</div>

{% endblock %}
